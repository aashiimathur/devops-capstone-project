#
# This pipeline needs the following tasks from Tekton Hub:
#   - git-clone
#   - flake8
#   - nose
#   - buildah
#   - kubernetes-actions (for oc apply or kubectl apply)
#
apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: cd-pipeline
spec:
  workspaces:
    - name: pipeline-workspace
  params:
    - name: repo-url
      description: URL of the Git repository
    - name: branch
      description: Git branch to clone
      default: main
    - name: build-image
      description: Image name to build and push (e.g. image-registry.openshift-image-registry.svc:5000/namespace/app:latest)
    - name: deploy-manifest
      description: Path to the Kubernetes deployment YAML
      default: deploy/deployment.yaml

  tasks:
    # Clean up workspace
    - name: init
      workspaces:
        - name: source
          workspace: pipeline-workspace
      taskRef:
        name: cleanup

    # Clone repository
    - name: clone
      workspaces:
        - name: output
          workspace: pipeline-workspace
      taskRef:
        name: git-clone
      params:
        - name: url
          value: $(params.repo-url)
        - name: revision
          value: $(params.branch)
      runAfter:
        - init

    # Run linting
    - name: lint
      runAfter:
        - clone
      taskRef:
        name: flake8
      workspaces:
        - name: source
          workspace: pipeline-workspace
      params:
        - name: image
          value: python:3.9-slim
        - name: args
          value:
            - --count
            - --max-complexity=10
            - --max-line-length=127
            - --statistics

    # Run tests using nose
    - name: tests
      runAfter:
        - clone
      taskRef:
        name: nose
      workspaces:
        - name: source
          workspace: pipeline-workspace
      params:
        - name: database_uri
          value: "sqlite:///test.db"
        - name: args
          value: "-v --with-spec --spec-color"

    # Build container image using Buildah
    - name: build
      runAfter:
        - lint
        - tests
      taskRef:
        name: buildah
        kind: ClusterTask
      workspaces:
        - name: source
          workspace: pipeline-workspace
      params:
        - name: IMAGE
          value: $(params.build-image)

    # Deploy to Kubernetes / OpenShift
    - name: deploy
      runAfter:
        - build
      taskRef:
        name: kubernetes-actions
      workspaces:
        - name: source
          workspace: pipeline-workspace
      params:
        - name: script
          value: |
            kubectl apply -f $(params.deploy-manifest)
